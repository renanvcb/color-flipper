# color-flipper
A simple html+javascript+css project that changes body background color in two ways:

1) Simple method:
A fixed collection of colors will be selected at random

2) Hex method:
A random hex code will be generated at random using values in a collection from 0 to "F".
